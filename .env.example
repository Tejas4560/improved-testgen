# AI TestGen - Environment Configuration Template
# Copy this file to .env and fill in your actual values

# ============================================
# LLM Configuration (Choose ONE option)
# ============================================

# Option 1: Azure OpenAI (Recommended for production)
AZURE_OPENAI_API_KEY=your-azure-openai-key-here
AZURE_OPENAI_ENDPOINT=https://your-endpoint.openai.azure.com/
AZURE_OPENAI_DEPLOYMENT=gpt-4
AZURE_OPENAI_API_VERSION=2024-02-15-preview

# Option 2: OpenAI API (Direct)
# OPENAI_API_KEY=your-openai-api-key-here
# OPENAI_MODEL=gpt-4

# Option 3: Ollama (Local LLM - Free)
# OLLAMA_HOST=http://localhost:11434
# OLLAMA_EMBED_MODEL=llama2
# VECTOR_DIM=768

# ============================================
# SonarQube Configuration (Optional)
# ============================================
SONAR_HOST_URL=https://sonarqube.example.com
SONAR_TOKEN=your-sonarqube-token
SONAR_PROJECT_KEY=my-project-key
SONAR_PROJECT_NAME=My Project Name

# ============================================
# Git Configuration
# ============================================
# Email and name for git commits
GIT_USER_EMAIL=your-email@example.com
GIT_USER_NAME=Your Name

# GitHub token for pushing generated tests (optional)
# Create at: https://github.com/settings/tokens
# Required scopes: repo
GIT_PUSH_TOKEN=your-github-personal-access-token

# ============================================
# Pipeline Configuration
# ============================================
# Minimum coverage threshold (default: 90)
MIN_COVERAGE_THRESHOLD=90

# Maximum iterations for test generation (default: 3)
MAX_ITERATIONS=3

# Target directory to analyze (relative to project root)
# TARGET_DIR=./app

# Output directory for generated tests
# OUTPUT_DIR=./tests/generated

# ============================================
# Advanced Configuration (Optional)
# ============================================
# Enable verbose logging
# VERBOSE=true

# Enable gap-focused mode (only generate tests for uncovered code)
# GAP_FOCUSED_MODE=true

# Coverage gaps file location
# COVERAGE_GAPS_FILE=./coverage_gaps.json

# Force test generation even if coverage is high
# TESTGEN_FORCE=false

# ============================================
# Notes
# ============================================
# 1. Never commit the actual .env file to git
# 2. Keep your API keys secure
# 3. For GitHub Actions, use repository secrets instead
# 4. Ollama requires local installation: https://ollama.ai/
